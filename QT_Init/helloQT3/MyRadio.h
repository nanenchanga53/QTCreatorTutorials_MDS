#ifndef MYRADIO_H
#define MYRADIO_H

#include "widget.h"

#include <QPushButton>

class QRadioButton;
class QButtonGroup;
class MyRadio : public QWidget
{
public:
    //QApplication qApp(argc,argv);
    //QPushButton *pushButton = new QPushButton("Exit(&E)", 0);
    //QObject::connect(pushButton,SIGNAL(clicked()),&qApp,SLOT(quit()));
    //pushButton->show();
};


#endif // MYRADIO_H
